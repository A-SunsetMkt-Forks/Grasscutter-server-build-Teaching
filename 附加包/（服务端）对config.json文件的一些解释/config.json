{								(开始的符号，可以理解为加载括号里的内容）
  "folderStructure": {						(各种文件的加载位置)
    "resources": "./resources/",				(加载在“resources文件夹res文件）（./代表当前目录下)
    "data": "./data/",						(跟上面的一样，保存加载data文件的路径，data存着服务端各种配置）
    "packets": "./packets/",					(跟上面一样）
    "scripts": "resources:Scripts/",				(保存加载路径，在根目录下的resources文件夹中的scripts（这个需要单独保存加载路径）里面有任务端必须的任务文件）
    "plugins": "./plugins/",					(加载plugins，也就告诉gc在plugins文件夹里放着插件，读取的时候去这里）
    "cache": "./cache/"						(仍然是保存加载位置）
  },								(同下)
  "databaseInfo": {						(数据库的端口）
    "server": {							(服务端在数据库的端口，大概）
      "connectionUri": "mongodb://localhost:27017",		(localhost其实就是127.0.0.1,后面的:27017指的是27017端口)
      "collection": "grasscutter"				(服务端在数据库的名称，数据库可不止能跑gc)
    },								(同下)
    "game": {							(游戏数据在数据库的端口，大概）
      "connectionUri": "mongodb://localhost:27017",		(同上)
      "collection": "grasscutter"				(同上)
    }								(结束符号，代表这一阶段结束了）
  },								(同上)
  "language": {			(关于语言配置)
    "language": "zh_CN",	(服务端语言zh_CN中文,en_US英文)
    "fallback": "en_US",	(这个其实不用管，这个是报错的语言，就算真报错你也看不懂)(总不可能是我不懂吧)
    "document": "EN"		(不懂，而且不用管)
  },				(同上)
  "account": {			(注册配置)
    "autoCreate": true,		(自动注册账号,密码，也就是所谓的账号密码随便输）
    "EXPERIMENTAL_RealPassword": false,		(启用密码验证，启用后需要输入指定密码，正常不开自动注册只要账号对，密码随便输都能进的)
    "defaultPermissions": [			(注册新账号默认权限"*"代表自动给与全部权限，记得加"")
      "*"					(同上)
    ],						(同上)
    "maxPlayer": -1		(服务端最多能有几个人,-1代表无限,就像mc的基岩硬度一样)
  },				(同上)
  "server": {			(服务端ip，连接等配置）
    "debugWhitelist": [],	(别动，调试白名单用的，如果你不是测试人员不要乱动)
    "debugBlacklist": [],	(别动，调试黑名单的，别乱动)
    "runMode": "HYBRID",	(运行类型，别乱动就对了，我说的别乱动不是我不教，是没有必要动，动的话大概率会启动失败)
    "logCommands": false,	(日志，用于记录服务端的各种事情，比如崩溃前输出)
    "http": {			(服务端ip类配置）
      "bindAddress": "0.0.0.0",	(地址绑定，没改的必要)
      "bindPort": 11451,	(服务端开在下面ip的哪个端口上）
      "accessAddress": "127.0.0.1",		(服务端开在这个ip上)
      "accessPort": 0,				(允许访问的端口，0为全部)
      "encryption": {				(密钥文件，用于让客户端登陆游戏)
        "useEncryption": true,			(是否启用密钥)
        "useInRouting": true,			(不懂，所以不用管)
        "keystore": "./keystore.p12",		(密钥文件的加载地址）
        "keystorePassword": "123456"		(密钥文件密码)
      },					(同上)
      "policies": {				(代理类型?????)
        "cors": {				(阿巴阿巴....)
          "enabled": false,			(是否启用，建议不要更改，因为不知道是什么)
          "allowedOrigins": [			(允许的起源?????)
            "*"					(应该是权限类型)
          ]					(同上)
        }					(上)
      },					(上)
      "files": {				(错误类型读取,不要修改)
        "indexFile": "./index.html",		(指数?)
        "errorFile": "./404.html"		(错误类型读取自根目录404.html)
      }						(上)
    },						(上)
    "game": {					(连接数据库配置）
      "bindAddress": "0.0.0.0",			(同上)
      "bindPort": 22102,			(数据库在27017端口)
      "accessAddress": "127.0.0.1",		(数据库ip为127.0.0.1）
      "accessPort": 0,				(允许访问的端口，0为全部)
      "loadEntitiesForPlayerRange": 100,	(实体加载范围，就是以玩家为中心半径100米内实体开始加载)
      "enableScriptInBigWorld": false,		(在大世界启用脚本，这个不要更改)
      "enableConsole": true,			(启用控制台，也就是可以在服务端的那个窗口输入指令，建议打开)
      "kcpInterval": 20,			(kcp（类似于tcp跟udp，一种传输协议）间隔）
      "logPackets": "NONE",			(好像是启用日志文件)
      "isShowPacketPayload": false,		(显示数据包负载?好像是防止过多发包的)
      "isShowLoopPackets": false,		(显示循环数据包?????????????????????????????????????????)
      "cacheSceneEntitiesEveryRun": false,	(每次运行缓存场景实体，也就是如果玩家超出加载范围，实体会被缓存，下次加载这个实体时会更快）
      "gameOptions": {				(玩家游戏设置)
        "inventoryLimits": {			(背包容量)
          "weapons": 2000,			(武器容量)
          "relics": 2000,			(圣遗物容量)
          "materials": 2000,			(材料容量)
          "furniture": 2000,			(尘歌壶家具容量)
          "all": 30000				(总共容量)
        },					(上)
        "avatarLimits": {			(队伍设置)
          "singlePlayerTeam": 4,		(玩家角色队伍人数上限)
          "multiplayerTeam": 4			(多人游戏玩家数量上限,一个世界)
        },					(上)
        "sceneEntityLimit": 1000,		(场景实体数量上限)
        "watchGachaConfig": false,		(显示抽卡记录,好像)
        "enableShopItems": true,		(启用商店购买物品)
        "staminaUsage": true,			(如果打开,体力值会消耗)
        "energyUsage": true,			(能源...的使用？）
        "fishhookTeleport": true,		(鱼钩传送，打开地图，然后标记的时候选择鱼钩，点确定后可以直接传送到标记地点)
        "trialCostumes": false,			(服装之类的，干啥我也不知道)
        "questing": {				(任务类)
          "enabled": true			(不要尝试更改这个，更改后100%报错，如果想关闭任务去res里面把任务文件删了就行)
        },					(上)
        "resinOptions": {			(树脂类)
          "resinUsage": false,			(树脂消耗?)
          "cap": 160,				(树脂上限,这里写的是160)
          "rechargeTime": 480			(一个树脂恢复的时间,以秒计算)
        },					(上)
        "rates": {				(经验获取倍率???)
          "adventureExp": 1.0,			(冒险经验)
          "mora": 1.0,				(摩拉)
          "leyLines": 1.0			(看不懂)
        },					(上)
        "handbook": {				(不用管，这是指令书的东西，可以直接用gctools代替）
          "enable": false,			(启用)
          "allowCommands": true,		(生成所有命令)
          "limits": {				(限制）
            "enabled": false,			(启用)
            "interval": 3,			(行间距，也就是每行之间的间隔)
            "maxRequests": 10,			(最多任务数量)
            "maxEntities": 25			(最多标题数量)
          },					(上)
          "server": {				(服务端设置，这个不需要更改，不需要更改！改了大概率报错)
            "enforced": false,			(启用/执行)
            "address": "127.0.0.1",		(ip)
            "port": 443,			(端口)
            "canChange": true			(可以更改)
          }					(上)
        }					(上)
      },					(上，后面的,代表结束，每一大行结束都需要加,)
      "joinOptions": {				(玩家进入游戏设置)
        "welcomeEmotes": [			(表情，也就是输指令那个地方那个机器人会发的表情，这里有三个说明会在这三个中任选其一发送)
          2007,					(看上面一行)
          1002,					(看上面两行)
          4010					(看上面三行)
        ],					(上)
        "welcomeMessage": "欢迎使用Gaobaiawa的3.7一键端",			(进入游戏后输指令那个地方的机器人刚进入游戏会给你发什么)
        "welcomeMail": {						(进入游戏后会给你发送一个邮件，这个邮件的内容)
          "title": "QQ群:158248452",					(邮件的标题)
          "content": "by - Gaobaiawa\n禁止倒卖\nQQ群:158248452\n游玩愉快\n\n你可以以以下任何一种形式找到我\ngithub:Gaobaiawa\nQQ:3054631349\n邮箱:gaobaiawa@163.com\ntwitter(因为看片被封了):@Gaobaiawa\ndiscord:gaobaiawa\nhoyoLAB:Nahida-quq",	(邮件内容)
          "sender": "Gaobaiawa",		(发送邮件的人)
          "items": [				(邮件的附件里面有什么)
            {					(上)
              "itemId": 13509,			(物品)
              "itemCount": 1,			(数量)
              "itemLevel": 1			(等级)
            },					(上)
            {					(上)
              "itemId": 201,			(第二个物品)
              "itemCount": 99999,		(的数量)
              "itemLevel": 1			(的等级)
            }					(上)
          ]					(上)
        }					(上)
      },					(上)
      "serverAccount": {			(发指令地方那个机器人设置)
        "avatarId": 10000073,			(机器人头像)
        "nameCardId": 210140,			(机器人名片，也就是打开好友看到那个纹路）
        "adventureRank": 500,			(冒险等级)
        "worldLevel": 500,			(世界等级)
        "nickName": "<color=#00FF00>Gaobaiawa</color>",			(机器人名称)
        "signature": "本一键端永久免费,qq群号158248452,如果你是购买的,说明你被骗了,请立即退款，遵循AGPL-3.0开源协议，除非本人允许，否则禁止以任何形式倒卖圈钱"							(机器人进入游戏给你发的话)
      },								(上)
      "visionOptions": [						(视觉修复,这个千万别动，除非你会修)
        {								(上)
          "name": "VISION_LEVEL_NORMAL",				(地图名字）
          "visionRange": 80,						(在这个地图的视觉范围,别调太高，除非你显卡够好)
          "gridWidth": 20						(在这个地图的网格宽度???)
        },								(上)
        {								(上)
          "name": "VISION_LEVEL_LITTLE_REMOTE",				(地图名字)
          "visionRange": 16,						(跟上面一样)
          "gridWidth": 40						(跟上面一样)
        },								(上)
        {								(上)
          "name": "VISION_LEVEL_REMOTE",				(地图名字)
          "visionRange": 1000,						(不想打字了,用。代替"跟上面一样")
          "gridWidth": 250						(。)
        },								(上)
        {								(上)
          "name": "VISION_LEVEL_SUPER",					(。)
          "visionRange": 4000,						(。)
          "gridWidth": 1000						(。）
        },								(。)
        {								(。)
          "name": "VISION_LEVEL_NEARBY",				(。)
          "visionRange": 40,						(。)
          "gridWidth": 20						(。)
        },								(。）
        {								(。)
          "name": "VISION_LEVEL_SUPER_NEARBY",				(。）
          "visionRange": 20,						(。)
          "gridWidth": 20						(。)
        }								(。)
      ]									(。)
    },									(。)
    "dispatch": {								(dispatch阶段)
      "regions": [],								(地区/区域?)
      "dispatchUrl": "ws://127.0.0.1:1111",					(dispatch的启动地址)
      "encryptionKey": "WL74WHFQRM3nD+ARLWJC0Vd3aMak4OXR0qT4BIYGW/w=",		(encryption密钥)
      "dispatchKey": "gEeNGtPsId2cRe4JGg2kqYAdZC+KgLQUhGMawysNpbQ=",		(dispatch密钥)
      "defaultName": "Grasscutter",						(默认名字)
      "logRequests": "NONE"							(日志请求)
    },										(。)
    "debugMode": {					(调试设置)
      "serverLoggerLevel": {				(服务器日志等级)
        "levelInt": 10000,				(级别整数)
        "levelStr": "DEBUG"				(名字,好像)
      },						(。)
      "servicesLoggersLevel": {				(。)
        "levelInt": 20000,				(。)
        "levelStr": "INFO"				(。)
      },						(。)
      "logPackets": "ALL",				(加载数据包限制)
      "isShowPacketPayload": false,			(显示数据包有效负载）
      "isShowLoopPackets": false,			(显示循环数据包)
      "logRequests": "ALL"				(日志请求)
    }							(。)
  },							(。)
  "version": 10						(版本）
}							(。)



											by - Gaobaiawa倒卖司马死全家
											qq群:158248452
											文件完全免费，如果你是购买的请举报+退款